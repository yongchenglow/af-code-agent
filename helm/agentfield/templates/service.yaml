apiVersion: v1
kind: Service
metadata:
  name: {{ include "agentfield.fullname" . }}
  labels:
    {{- include "agentfield.labels" . | nindent 4 }}
spec:
  type: {{ .Values.controlPlane.service.type }}
  ports:
    - port: {{ .Values.controlPlane.service.port }}
      targetPort: http
      protocol: TCP
      name: http
      {{- if and (eq .Values.controlPlane.service.type "NodePort") .Values.controlPlane.service.nodePort }}
      nodePort: {{ .Values.controlPlane.service.nodePort }}
      {{- end }}
  selector:
    {{- include "agentfield.selectorLabels" . | nindent 4 }}
