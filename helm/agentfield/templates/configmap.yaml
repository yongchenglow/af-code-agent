apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "agentfield.fullname" . }}-config
  labels:
    {{- include "agentfield.labels" . | nindent 4 }}
data:
  # Environment is set via the secret (agentfield-secrets)
  # Add any additional non-sensitive configuration here
  ENVIRONMENT: "production"
